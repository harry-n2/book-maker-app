# Book Maker v2 改良 設計図（電子書籍専用・仕様のみ・実装なし）

> 作成: 2026-06-20 ／ 状態: **設計図ドラフト（オーナー承認待ち）**
> スコープ確定（2026-06-20 オーナー指示）：**電子書籍（原稿）生成のみ。マンガ化・表紙生成は対象外。** 画像生成エンジン(OpenAI等)も対象外。
> 根拠: 既存コード実地監査（app.py / generator.py / prompts ほか）＋ fusion 合議。
> このファイルは仕様のみ。実装・デプロイはオーナーの個別指示まで行わない。マンガ仕様は `docs_archive/REDESIGN_v2_manga_spec.OUT_OF_SCOPE.md` に退避。

---

## 1. 方針（一言で）

**今の「テーマ→タイトル→章立て→本文」アプリを、(1)サーバーレスで壊れない (2)企画・編集・販売文の質が高い (3)初心者が迷わない (4)同じ入力で同じ品質が出る、の4点で劇的改良する。** 単一アプリ・単一リポジトリのまま（機能分割は不要）。

---

## 2. 現状の確定事実（一次確認済・証跡つき）

| 項目 | 内容 | 証跡 |
|---|---|---|
| 構成 | FastAPI + Gemini(BYOK) + バニラJS。4段階HITL（素材→タイトル10選→章立て→本文） | `app.py` / `index.html` |
| モデル | `gemini-2.0-flash` 固定（旧世代） | `generator.py:32` |
| 出力 | md / docx(KDP) / タイトル10選 / 紹介文 / NotebookLM | `app.py:572-579` |
| 反捏造 | 「プロフィール外の実績・数字を作らない」をプロンプト全体で徹底（強み） | `prompts/*.txt` |

---

## 3. 直すべき弱点（監査で確定・電子書籍スコープ）

1. **🔴 Vercelでジョブが壊れる**：`JOB_STATE`(プロセス内dict, `app.py:45`)＋`threading`(416/475/531)はサーバーレスで状態消失・スレッド破棄。README自身が「Vercel版は制限→ローカル版を使え」と認める＝本番が劣化版。
2. **🟠 4役割が未統合**：紹介文は汎用（`description.txt`）でPASONA/頭脳DB未使用。章は4000字未強制。タイトルはテーマのみで企画部長のバズ/競合分析が無い。
3. **🔴 モデルが停止済みの疑い**：`gemini-2.0-flash` 固定（`generator.py:32`）。fusion(Codex)のGoogle公式pricing参照では **2026-06-01 shutdown 済み**との指摘あり→事実なら**現アプリは今すでに生成不能**。最優先で一次確認＋モデル更新（品質ティア化）。
   - 推奨（要・公式doc一次確認）: economy=`gemini-3.1-flash-lite` / standard=`gemini-3.5-flash` / premium=`gemini-3.1-pro` or `gemini-2.5-pro`。`latest`エイリアス禁止・`model_id`を固定保存（再現性）。
4. **🟠 初心者向けでない**：初期画面に必須5項目＋任意多数。おまかせ/テンプレ/記入例/安心進捗が無い。

---

## 4. 改良後アーキテクチャ（単一アプリ）

```
[単一UI / FastAPI]  ← ユーザーには1つの「本を作るアプリ」
   │ BookJobSpec（1冊=1つの再実行可能な仕様）
   ├─ 工程0: 企画(企画部長)  … 市場/競合/売れる切り口3案
   ├─ 工程1: タイトル(編集者) … 8-10案
   ├─ 工程2: 章立て          … 4-5章・各章sections
   ├─ 工程3: 本文(章ごと)     … 各章4000字+・反捏造維持
   ├─ 工程4: はじめに/紹介文(頭脳) … DRM/PASONA
   └─ 出力: md / docx(KDP) / 紹介文 / NotebookLM
   [Postgres or KV: ジョブ状態/章チェックポイント/各種版]
   [Blob: 原稿/中間成果物]   [QStash: 章ごと冪等実行]
```
- **章ごとにステップ化**し、失敗章だけ再開（Vercel実行時間に収める）。テキストのみなので画像系の重い処理は無い＝インフラは軽量で足りる。
- リポジトリは現 `book_maker_app` を土台に段階移行（反捏造プロンプト資産を温存）。

---

## 5. 再現性の中核
- **BookJobSpec**：入力全文＋`input_hash` / `prompt_version`(上書き禁止・追加のみ) / `model_name+version` / params / 参照素材ID / `pipeline_version`。全成果物に保存。
- **冪等キャッシュ**：同一ジョブキーは再生成せず保存済みを返す（QStashは最低1回実行→二重生成防止）。
- **章ごとゴールデンテスト**をCIで。評価は「同じ品質基準を満たすか」。

## 6. 4役割の統合（電子書籍版・表紙デザイナーは対象外）
- **企画部長**＝工程0：テーマ→読者/競合/売れる切り口を提案（おまかせの中核）。
- **編集者**＝タイトル8-10案・各章4000字+・PASONA紹介文1000字+。
- **頭脳**＝「はじめに」「紹介文」をDRM 28パート構造（危機認識→ベネフィット→CTA）で強化。

## 7. 初心者UX
- 入口1つ。「テーマ1行」だけ先に聞き、残りはおまかせ＋折りたたみ。
- テンプレギャラリー（ジャンル別プリセット）＋記入例ワンタップ＋素材の薄さ警告。
- 進捗バー＋残り時間／停止・再開／**失敗章だけ再生成**／完成後「KDPにこの順で出す」3ステップ。
- デザインは claude-design＋artifact-design＋brand-assets（slate一色脱却・プロ品質）。

## 8. 10ラウンド改善ロードマップ（fusion合議で確定・Codex全10成功/Gemini429失敗）

> 各ラウンド共通手順：①1ページ仕様要約 → ②fusionで弱点3個 → ③採点(重大度/発生確率/修正容易性/売上影響/法務リスク) → ④上位3つ確定(合議) → ⑤ローカル実装＋章ゴールデンテスト＋実機スモーク → ⑥回帰ゲート → ⑦合格後に1デプロイ→本番検証→ログ追記。P0/P1残存ならリリースしない。

| R | 狙う弱点 | 主な仕様 | 検証 |
|---|---|---|---|
| R1 | Vercelでジョブ揮発・復旧不能 | Job/Step/Artifact を**Postgres永続化**、章ごとチェックポイント、各工程idempotent | 再起動・二重実行・途中失敗から同一成果物に復帰 |
| R2 | 同一入力で再現不可 | **Run Manifest**（入力/素材ID/prompt版/model_id/温度/seed/出力）を全工程保存 | golden inputで差分比較・劣化検出 |
| R3 | 実績/数字の混入 | **反捏造エビデンス台帳**（主張を 素材由来/ユーザー確認/推論/禁止 に分類） | 未根拠の数字・実績が本文/紹介文に出ない |
| R4 | 企画品質・市場性不足 | **企画部長フェーズ**を前段追加（読者/悩み/約束/差別化/切り口3案）。必須入力は3つ＋おまかせ | 企画スコア・章立てへの反映率 |
| R5 | タイトル/紹介文が汎用 | タイトル10選を型別＋**検索性/刺さり/信頼性/反捏造**で採点。PASONAは"配置"だけ・主張は台帳から | 人間レビューで上位案が納得・誇張なし |
| R6 | 章構成が平板・重複 | **編集者フェーズ**：各章に役割/問い/読者変化/根拠素材/避ける内容 | 重複率・論理飛躍・未回収テーマ |
| R7 | 本文の文体ブレ・章ムラ | 本文を**章ブリーフ→小節分割→ブロック生成→密度検証→増補→統合編集**の製造ラインに。Style Bible/用語集を各章注入 | 固有名詞・数字・語調・章間整合の回帰 |
| R8 | 品質判断が主観 | **品質評価エンジン**（企画力/独自性/信頼性/読みやすさ/販売力をスコア化）。低スコア箇所だけ再生成 | 改善前後の差分保存 |
| R9 | モデル停止/陳腐化 | **Model Adapter**（economy/standard/premium/audit を工程別に解決）。`gemini-2.0-flash`脱却 | 同一golden inputで新旧比較・採用基準明文化 |
| R10 | 初心者UX・KDP入稿前不足 | おまかせ/テンプレ/記入例/進捗安心/失敗章だけ再生成。**KDP出力ゲート**（見出し階層/目次/奥付/紹介文4000字/AI生成申告lint） | 初回ユーザーが企画承認→Word出力まで迷わず完了 |

> ★ R8/R10で出たKDP適合の要点：DOCXはWord見出しスタイル(H1/H2/H3)・章頭改ページ・Interactive TOC・固定奥付テンプレを使う。紹介文はランキング主張/誇大/内容不一致をlint。**AI生成コンテンツはKDP出版画面で申告必要**→申告メモを自動同梱（出典: KDP公式・実装時に再確認）。

## 9. 次の一手（実装ではない）
1. **BookJobSpec v1 の仕様書（JSON契約md・コードなし）** を固める。
2. fusionで電子書籍アプリの弱点を10ラウンド洗い出し→改善計画化。
3. 承認後にラウンド1（基盤修復）から実装（1デプロイ）。

> 実装・デプロイ・本番反映は毎回オーナー承認後のみ。捏造報告・推測修正・本番反復はしない。
